# T: from fix.bud
#
# by Michael Libourel
# $Revision: 1.1 $

version 1
struct Root {
    root is of fixed
    struct {
        string { length 1; match <A>; } uno;
        string { length 2; } dos;
    } one[];
    string { length 4; } two[];
};
