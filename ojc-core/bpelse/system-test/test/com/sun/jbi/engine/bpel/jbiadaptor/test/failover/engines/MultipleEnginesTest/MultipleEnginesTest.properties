# deployed artifacts location      
testfolder=bpels/assign/
deploymentDirectory=bpels/assign/deployedFolder

# Inbound Message endpoint info (sevice, endponit, operation), input message file and input message type defining the message to send to the IMA endpoint
Input1={urn:assign:assignService}caller, caller_myRole, assign, input/input1.xml, {urn:assign:assignService}AssignMessageType

# Outbound Message endpoint info (sevice, endponit, operation), output file to compare against the output received
Output1={urn:assign:assignService}InvokePLT, AssignInvokeRole_partnerRole, oneWayInvokeOper, output/output1.xml