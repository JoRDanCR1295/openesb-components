# This test sends same message to two engines.
# The following defines how the messages will be routed to which engine.
engine1.messages=Input1, Input1, Input1, Input1, Input1, Input1, Input1, Input1, Input1, Input1, Input1, Input1, Input1, Input1
#engine1.messages=Input1, Input1

# Define the message to engine mapping here
expectedCompleteInstances=14

engine1.crashpoint=FunctionalTest2.bpel, 20

# the durtaiton after which this engine would shutdown, regardless of the fact the messages
# were processed or not. Time duration in seconds
engine1.runDuration=20


recoveryBatchSize=4