<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">

  <modelVersion>4.0.0</modelVersion>

  <!--
  =========================================================
                                                COORDINATES
  =========================================================
  -->
  <parent>
    <groupId>it.imolinfo.jbi4corba.test-provider-crb106</groupId>
    <artifactId>test-provider-crb106</artifactId>
    <version>1.0-SNAPSHOT</version>
  </parent>
  <artifactId>provider-crb106-service-assembly</artifactId>

  <!--
  =========================================================
                                                DESCRIPTION
  =========================================================
  -->
  <name>${pom.groupId}.${pom.artifactId}-${pom.version} # ${pom.packaging}</name>

  <!--
  =========================================================
                                                  PACKAGING
  =========================================================
  -->
  <packaging>jbi-service-assembly</packaging>

  <dependencies>
    
    <dependency>
      <groupId>${pom.groupId}</groupId>
      <artifactId>provider-crb106-jbi4corba-provider-single</artifactId>
      <version>${pom.version}</version>
    </dependency>
    
    <dependency>
      <groupId>${pom.groupId}</groupId>
      <artifactId>provider-crb106-jbi4corba-provider-double</artifactId>
      <version>${pom.version}</version>
    </dependency> 
    
    <dependency>
      <groupId>${pom.groupId}</groupId>
      <artifactId>provider-crb106-http-consumer-single</artifactId>
      <version>${pom.version}</version>
    </dependency>
    
    <dependency>
      <groupId>${pom.groupId}</groupId>
      <artifactId>provider-crb106-http-consumer-double</artifactId>
      <version>${pom.version}</version>
    </dependency>
    
  </dependencies>

</project>